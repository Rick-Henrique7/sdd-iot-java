# Change 013 — tasks

- [x] 1. Branch `013-frontend-polish-and-readme` and write the 4
  SDD artifacts.
- [x] 2. Fix orthography across the 17 affected frontend files
  (see `design.md` for the per-file list).
- [x] 3. Layout fixes in `Mapping.tsx` and `OpenMeteoWidget.tsx`
  (legend gap, min-h, widget width).
- [x] 4. README Section 3: 4 new tech-stack rows + 3 new
  shields.
- [x] 5. README Section 7.1 (Methodology) and Section 8.1
  (How to test).
- [x] 6. `npm test` — stays at 52/52 (or grows if formatRelative
  test is added).
- [x] 7. `npm run build` — green.
- [x] 8. Rebuild `agrio-frontend-shell` image and recreate the
  container.
- [x] 9. Commit, push, open PR, wait for CI, squash-merge,
  archive to `changes/archive/2026-08-16-013-frontend-polish-and-readme/`.
- [x] 10. Manual smoke: visit `/dashboard`, `/mapping`, `/fleet`,
  `/settings`; confirm accents render and legend reads with
  clear gaps.
