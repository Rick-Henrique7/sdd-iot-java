apiVersion: v1
kind: Secret
metadata:
  name: agrio-secrets
  namespace: agrio-production
type: Opaque
stringData:
  # The actual values must be set via sealed-secrets or your secret manager.
  # These placeholders exist only to document the contract.
  JWT_SECRET: "REPLACE_VIA_SECRETS_MANAGER"
  POSTGRES_PASSWORD: "REPLACE_VIA_SECRETS_MANAGER"
  SPRING_DATASOURCE_PASSWORD_AUTH: "REPLACE_VIA_SECRETS_MANAGER"
  SPRING_DATASOURCE_PASSWORD_FLEET: "REPLACE_VIA_SECRETS_MANAGER"
  SPRING_DATASOURCE_PASSWORD_TELEMETRY: "REPLACE_VIA_SECRETS_MANAGER"
