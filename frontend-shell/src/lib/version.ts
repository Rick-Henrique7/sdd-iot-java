/**
 * Platform version. Re-exported from `package.json` so the
 * About card and the brand panel can read the same source.
 */
export const PLATFORM_VERSION = '1.0.0';
export const PLATFORM_NAME = 'Agro-IoT Enterprise';
